# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

### 新增
- **召回引擎可切换**：关键词匹配 / 语义向量（`core/semantic_funnel.py`）
  - 关键词匹配（默认）：零延迟、完全离线，靠手写词表字面比对
  - 语义向量：把上下文与音效描述转成向量按相似度召回，能识别同义不同词
  - 语义服务不可用时**自动回退关键词召回**，功能不中断
  - 设置页内含详细说明与「测试 embedding」按钮
- **触发灵敏度三段式开关**：保守 / 均衡 / 激进
  - 同时控制「零命中是否本地拦截」与「提示词语气倾向」
- **版本号单一来源**：`core/__init__.py` 的 `__version__`，
  GUI 与 `pyproject.toml` 均从此读取，杜绝版本漂移
- **扩充音效库至 50 个**：新增 27 个新三国名场面梗，覆盖更多开黑场景
  - 庆祝类：接着奏乐接着舞、狂徒
  - 阴阳反讽类：我爱死他了、各各有情有义、宁肯天下人负我
  - 破防怒骂类：曹贼奸贼恶贼逆贼、吃汝肉、一万个透明窟窿、这帮鸟人、放肆、狂徒
  - 翻车类：煮熟的鸭子飞走了
  - 甩锅耍赖类：知错改错不认错、敢赖会赖一赖到底
  - 驱逐处决类：叉出去、给我推出去砍了砍了
  - 单挑挑衅类：敢与我决斗吗、胆敢搜我的身我砍你的头
  - 摸鱼摆烂类：我打了一辈子仗就不能享受享受嘛、董卓呼噜
  - 其他：春秋胡言乱语、刘备匹夫、董卓大笑、我的头颅只值五十金、先帝爷、给你个鸡毛
- **截图分辨率可选**：256 / 384 / 512 / 768，直接控制图片 Token 开销
- **视觉模型可解耦**：支持复用主大脑或单独配置更便宜的视觉渠道
- **线程生命周期管理**：所有监听器 stop() 现在会真正 join 线程

### 修复
- **停止后线程残留**（重要）：`stop()` 只置标志位不 join，而采样线程最长休眠 30 秒，
  导致停止后线程仍存活、重启后新旧线程并存抢占音频/屏幕设备。现改为可中断休眠 + join，
  stop() 从最长 30 秒降至毫秒级返回
- **`requests.Session` 跨线程共享**（重要）：官方文档明确 Session 非线程安全，
  而本项目的抽帧/语音/截图三个线程共用同一个实例。现改为**每线程独立 Session**
  （线程局部存储），既消除竞争又保留同线程内的连接复用
- **Token 计数器并发丢更新**：`session_* +=` 无锁保护，多线程并发时统计偏少。
  现加锁保护并提供 `stats_snapshot()` 原子读取
- **音效编辑保存失效**（重要）：PyQt5 的 `item.data()` 返回 dict 深拷贝而非本体，
  编辑/删除操作只改到副本，`meme_library.json` 实际未被更新。现按 id 回查真实对象
- **规则表 id 错位**：原按数字 id 绑定关键词规则，用户增删音效后 id 重排会导致规则整体错位，
  出现「说五杀却播放《死是凉爽的夏夜》」。现改为按标题绑定
- **提示词「过度高冷」**：原「约 8 成应沉默」的表述导致大量该触发的场景被漏掉，
  且零命中时漏斗会直接拦截、根本没送到 LLM 面前
- **版本号漂移**：GUI 显示 v1.0.0 而 pyproject 已到 1.1.0。现统一由
  `core/__init__.py` 的 `__version__` 提供，并加测试锁定
- **独立视觉模型 Token 未计入会话账单**
- **死配置键清理**：移除代码从未读取的 `api.provider` 与 `behavior.enable_silence_option`

### 文档
- 重写 README 中失效的描述（音效数量、零命中行为），并**详细说明漏斗的工作原理**
  - 两种召回引擎的原理、优缺点、适用场景
  - 语义向量模式的服务配置方法与自动回退行为
  - 图片 Token 的计费差异（官方 tile 计费 vs 反代 base64 折算）
  - 按影响排序的 Token 优化排查清单
- `config.example.json` 补全至与运行时 schema 完全一致，并加测试防止配置漂移

### 变更
- 版本号提升至 1.2.0

### 计划中
- 拆分 `gui.py`（当前约 2400 行，计划拆为独立页面模块）
- 增加音频设备热插拔自动恢复
- 支持按游戏类型配置 ROI 截图区域

---

## [1.1.0] - 当前版本

工程化与健壮性完善版本。

### 新增
- **网络层重试与熔断** (`core/http_client.py`)
  - 指数退避重试，支持 429 / 5xx / 超时等瞬态故障
  - 优先遵守服务端 `Retry-After` 头
  - 连续失败自动熔断，冷却后进入半开试探
  - 连接池复用，减少握手开销
- **统一日志系统**：以 `logging` 替代散落的 `print`，支持输出到 `logs/automeme.log`
- **单元测试套件** (`tests/`)：65 项测试，覆盖 Token 解析、漏斗召回、上下文冷却、HTTP 重试与熔断
- **零依赖测试运行器** (`tests/run_all.py`)：无 pytest 环境亦可执行
- **项目基础设施**：`LICENSE`（MIT + 第三方组件声明）、`CHANGELOG.md`、完善的 `.gitignore`

### 优化
- **两段式漏斗粗筛**：先本地筛选候选再送大模型，单次 Prompt 从约 2000 字压缩到 300 字
  - 零命中时直接本地静默，完全不发起网络请求
  - 关键词表与内置规则引擎共用同一数据源，避免两套词库漂移
- **可调截图分辨率**：256 / 384 / 512 / 768，直接控制图片 Token 开销
- **推理过程输出改为可开关**：默认关闭，输出 Token 降低约 80%
- **缓存统计兼容多厂商**：支持 OpenAI / DeepSeek / Anthropic / Gemini 的字段命名差异

### 修复
- **规则表按标题绑定**（重要）：原按数字 id 绑定规则，用户在梗库删除音效后 id 重排会导致规则整体错位，
  出现「说五杀却播放《死是凉爽的夏夜》」这类错误。改为标题匹配后彻底解决。
- **`_heuristic_decide` 变量未定义**：重构遗留的 `rules` 引用会导致内置规则引擎抛异常
- **配置回写覆盖**：程序启动时 `_load_settings` 触发控件回调，会把默认值写回磁盘覆盖用户配置
- **徽章归零**：单次 0 Token 消耗时会把会话累计显示重置为 0
- **独立视觉模型 Token 未计入**：视觉模型的消耗遗漏在会话账单之外
- **漏斗零命中回退全量清单**：带图场景下候选为空时会回退到 1700 字全量清单，导致优化失效
- **音效文件定位失败**：`打开文件位置` 无法穿透子目录查找音频

### 变更
- `config.json` 加入 `.gitignore`，避免 API Key 与局域网地址被提交
- 提供 `config.example.json` 作为开源分发模板
- `requirements.txt` 补全缺失依赖

---

## [1.0.0] - 初始版本

- 双通道音频感知（麦克风 + WASAPI 扬声器回环）
- 基于 SenseVoice-Small + Silero VAD 的离线中英双语识别
- RapidOCR 屏幕文字检测（阵亡 / 胜利 / 任务失败）
- 大模型语境裁决与内置规则引擎双模决策
- PyQt5 桌面界面：控制台 / 音效库 / 决策审视 / 设置
- 会话 Token 结算报告
- 音效库自定义管理
